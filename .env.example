# Concieragent Environment Variables
# Copy this file to .env and fill in your actual API keys

# Docker Hub (required for CI/CD)
DOCKER_HUB_LOGIN=your-dockerhub-username
DOCKER_HUB_PWD=your-dockerhub-password-or-token

# Kubernetes (required for CI/CD)
KUBECONFIG=<base64-encoded-kubeconfig>

# ═══════════════════════════════════════════════════════════════════════════
# LLM Provider Configuration
# ═══════════════════════════════════════════════════════════════════════════

# Choose provider: 'openai', 'claude', or 'ollama'
LLM_PROVIDER=openai

# OpenAI (default)
OPENAI_API_KEY=your-key
# OPENAI_MODEL=gpt-4o  # optional

# Claude
# ANTHROPIC_API_KEY=your-key
# CLAUDE_MODEL=claude-sonnet-4-20250514  # optional

# Ollama (no API key needed, just run 'ollama serve')
# OLLAMA_BASE_URL=http://localhost:11434  # optional
# OLLAMA_MODEL=llama3.1  # optional

# ═══════════════════════════════════════════════════════════════════════════
# MCP Server API Keys
# ═══════════════════════════════════════════════════════════════════════════

# SerpAPI Key (required for flight, hotel, event, and finance searches)
# Get your key from: https://serpapi.com/
# Free tier: 100 searches/month
SERPAPI_KEY=your-key

# OpenWeather API Key (required for weather data)
# Get your key from: https://openweathermap.org/api
OPENWEATHER_API_KEY=your-key

# ═══════════════════════════════════════════════════════════════════════════
# VS Agent Configuration
# ═══════════════════════════════════════════════════════════════════════════

# VS Agent Admin API URL (optional, defaults to http://localhost:3000)
# Only change if VS Agent is running on a different host/port
VS_AGENT_URL=http://localhost:3000

# ═══════════════════════════════════════════════════════════════════════════
# Storage Configuration (Persistent Message History)
# ═══════════════════════════════════════════════════════════════════════════

# Storage provider: 'memory' (default, non-persistent) or 'postgres' (persistent)
STORAGE_PROVIDER=memory

# PostgreSQL Configuration (required when STORAGE_PROVIDER=postgres)
# For local development, run: docker run -d --name concieragent-postgres \
#   -e POSTGRES_DB=concieragent -e POSTGRES_USER=concieragent \
#   -e POSTGRES_PASSWORD=your-password -p 5432:5432 postgres:16
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=concieragent
POSTGRES_USER=concieragent
POSTGRES_PASSWORD=your-password

# Redis Configuration (optional, improves performance with caching)
# For local development, run: docker run -d --name concieragent-redis -p 6379:6379 redis:7
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Session expiration in days (default: 7)
# SESSION_EXPIRATION_DAYS=7
